apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup1-pv-slave-claim
  namespace: velero
spec:
  resources:
    requests:
      storage: 2Ti
  storageClassName: local-storage
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
